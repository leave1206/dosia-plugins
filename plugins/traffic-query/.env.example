# 复制为 ~/.zshrc 中的 export，或本地 .env（已 gitignore）
# 不要提交此文件包含真实凭据的版本

# 必填：MongoDB 连接串
export MONGODB_URI="mongodb://admin:PASSWORD@HOST:27017/admin?authSource=admin"

# 可选：HTTP 模式（迁移到另一台 Mac 时使用）
# export HTTP_MODE=1
# export PORT=8080
# export SERVICE_TOKEN=your-random-token-here
